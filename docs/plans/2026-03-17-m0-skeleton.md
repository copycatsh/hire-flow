# M0 Skeleton Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** All 7 backend services boot in Docker Compose, each responds to `/health`, `make health` passes.

**Architecture:** Monorepo with Go workspace (go.work). 4 core services (jobs-api/Echo, contracts/Chi, payments/Gin, ai-matching/FastAPI) + 3 BFF services (stdlib). Traefik v3 routes to BFFs via path prefix. Two PostgreSQL containers (one per PG-backed service), MySQL, NATS JetStream, Qdrant as infrastructure. Docker healthchecks on all containers.

**Tech Stack:** Go 1.24 (Echo v4, Chi v5, Gin, stdlib), Python 3.12+ (FastAPI), Docker Compose, Traefik v3

**Decisions from eng review:**
- Two PostgreSQL containers: `postgres-jobs` (:5432), `postgres-payments` (:5433)
- CLAUDE.md written from scratch (not copied from arch doc template)
- Test strategy: `make health` curl-based integration only (no unit tests for M0)
- Docker healthchecks on all service containers in compose.yaml
- Flat Go package structure (no hexagonal folders)
- `pkg/` shared packages deferred to M1+

---

## Service Port Map

```
Service            Port   Framework   Database
─────────────────────────────────────────────────
jobs-api           8001   Echo v4     postgres-jobs
ai-matching        8002   FastAPI     Qdrant
contracts          8003   Chi v5      MySQL
payments           8004   Gin         postgres-payments
bff-client         8010   stdlib      —
bff-freelancer     8011   stdlib      —
bff-admin          8012   stdlib      —

Infrastructure     Port
─────────────────────────────────────────────────
Traefik HTTP       80
Traefik Dashboard  8080
postgres-jobs      5432
postgres-payments  5433
MySQL              3306
NATS client        4222
NATS monitoring    8222
Qdrant HTTP        6333
Qdrant gRPC        6334
```

## Request Flow (M0)

```
                       ┌──────────────┐
                       │  Traefik :80 │
                       └──────┬───────┘
              ┌───────────────┼───────────────┐
              │               │               │
     /client/*        /freelancer/*     /admin/*
     (strip prefix)   (strip prefix)   (strip prefix)
              │               │               │
     ┌────────▼──┐   ┌───────▼───┐   ┌───────▼──┐
     │bff-client │   │bff-freelan│   │bff-admin │
     │   :8010   │   │   :8011   │   │   :8012  │
     └───────────┘   └───────────┘   └──────────┘

  Internal services (direct port access, no Traefik):
     jobs-api:8001  ai-matching:8002  contracts:8003  payments:8004
```

---

## Task 1: Foundation — Directory Structure + go.work + CLAUDE.md

**Files:**
- Create: `go.work`
- Create: `CLAUDE.md`
- Create: `TODOS.md`
- Create: `services/jobs-api/.gitkeep` (directories)
- Create: `services/contracts/.gitkeep`
- Create: `services/payments/.gitkeep`
- Create: `services/bff/client/.gitkeep`
- Create: `services/bff/freelancer/.gitkeep`
- Create: `services/bff/admin/.gitkeep`
- Create: `services/ai-matching/.gitkeep`
- Create: `infra/traefik/.gitkeep`
- Create: `infra/nats/.gitkeep`
- Create: `infra/postgres/.gitkeep`
- Create: `infra/mysql/.gitkeep`

**Step 1: Create directory structure**

```bash
mkdir -p services/{jobs-api,contracts,payments,ai-matching}/cmd/server
mkdir -p services/bff/{client,freelancer,admin}/cmd/server
mkdir -p infra/{traefik,nats,postgres,mysql}
```

**Step 2: Create go.work**

```go
// file: go.work
go 1.24

use (
	./services/jobs-api
	./services/contracts
	./services/payments
	./services/bff/client
	./services/bff/freelancer
	./services/bff/admin
)
```

Note: go.work cannot include Python service. The `use` directives will error until go.mod files exist — that's expected at this step.

**Step 3: Create CLAUDE.md**

```markdown
# hire-flow — AI Hiring Platform (Microservices)

## Quick Start
make up          # Start all services
make down        # Stop all
make health      # Check all services respond to /health
make logs s=jobs # Logs for specific service
make test        # Run all tests

## Architecture
Monorepo with 4 core services + 3 BFFs + 1 Python ML service.
See docs/architecture/overview.md for full details.

## Conventions
- Go services: flat package structure (group by feature, not layer)
- Interfaces belong to the consumer, not the provider
- Python service: FastAPI with src/ layout
- Each service has own database, NO cross-service DB access
- Events via NATS JetStream (future milestones)
- Outbox pattern for reliable event publishing (future milestones)

## Go Version
Go 1.24 — use modern features: enhanced ServeMux patterns, t.Context() in tests,
omitzero in JSON tags, SplitSeq/FieldsSeq for iteration.

## Service Ports (local dev)
| Service          | Port | Framework |
|------------------|------|-----------|
| Traefik HTTP     | 80   | —         |
| Traefik Dashboard| 8080 | —         |
| jobs-api         | 8001 | Echo v4   |
| ai-matching      | 8002 | FastAPI   |
| contracts        | 8003 | Chi v5    |
| payments         | 8004 | Gin       |
| bff-client       | 8010 | stdlib    |
| bff-freelancer   | 8011 | stdlib    |
| bff-admin        | 8012 | stdlib    |
| postgres-jobs    | 5432 | —         |
| postgres-payments| 5433 | —         |
| MySQL            | 3306 | —         |
| NATS client      | 4222 | —         |
| NATS monitoring  | 8222 | —         |
| Qdrant HTTP      | 6333 | —         |
| Qdrant gRPC      | 6334 | —         |

## Go Conventions
- Error handling: wrap with fmt.Errorf("operation: %w", err)
- Naming: domain types are NOT prefixed (Job, not JobEntity)
- Tests: table-driven, testify for assertions
- Each service has own go.mod (Go workspace via go.work at root)
- No internal/ unless service grows beyond ~15 files

## Python Conventions
- Python 3.12+, FastAPI
- pydantic v2 for models
- pytest for tests
- ruff for linting

## Docker
- compose.yaml (not docker-compose.yml)
- One process per container
- Multi-stage builds for Go services
- Docker healthchecks on all containers
```

**Step 4: Create TODOS.md**

```markdown
# TODOS

## Pending

### Fix architecture doc contradictions
- **What:** Update docs/architecture/overview.md to fix stale/contradictory info
- **Why:** Arch doc says `docker-compose.yml` (should be `compose.yaml`), says Go 1.22+ (should be 1.24), CLAUDE.md template references hexagonal architecture (should be flat Go structure), shows `pkg/` shared packages (deferred)
- **Context:** CLAUDE.md at root is now authoritative for conventions. Arch doc is the canonical design reference but has drifted from actual implementation decisions made during M0 eng review.
- **Depends on:** M0 complete
- **Priority:** Low — fix before M1

## Completed
(none yet)
```

**Step 5: Commit**

```bash
git add go.work CLAUDE.md TODOS.md services/ infra/ docs/plans/
git commit -m "feat(m0): foundation — directory structure, go.work, CLAUDE.md"
```

---

## Task 2: Infrastructure Config Files

**Files:**
- Create: `infra/traefik/traefik.yml`
- Create: `infra/nats/nats-server.conf`
- Create: `infra/postgres/init-jobs.sql`
- Create: `infra/postgres/init-payments.sql`
- Create: `infra/mysql/init.sql`

**Step 1: Create Traefik static config**

```yaml
# file: infra/traefik/traefik.yml
api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    exposedByDefault: false
    network: hire-flow

log:
  level: INFO
```

**Step 2: Create NATS config**

```conf
# file: infra/nats/nats-server.conf
listen: 0.0.0.0:4222
http: 0.0.0.0:8222

jetstream {
  store_dir: /data/jetstream
  max_mem: 256MB
  max_file: 1GB
}
```

**Step 3: Create PostgreSQL init scripts**

```sql
-- file: infra/postgres/init-jobs.sql
CREATE DATABASE jobs_db;
```

```sql
-- file: infra/postgres/init-payments.sql
CREATE DATABASE payments_db;
```

**Step 4: Create MySQL init script**

```sql
-- file: infra/mysql/init.sql
CREATE DATABASE IF NOT EXISTS contracts_db;
```

**Step 5: Commit**

```bash
git add infra/
git commit -m "feat(m0): infrastructure config files — traefik, nats, postgres, mysql"
```

---

## Task 3: compose.yaml — Infrastructure Services

**Files:**
- Create: `compose.yaml`

**Step 1: Create compose.yaml with infrastructure only**

```yaml
# file: compose.yaml
name: hire-flow

networks:
  hire-flow:
    driver: bridge

volumes:
  postgres-jobs-data:
  postgres-payments-data:
  mysql-data:
  nats-data:
  qdrant-data:

services:
  # ── Infrastructure ──────────────────────────────────────────

  traefik:
    image: traefik:v3.3
    container_name: hire-flow-traefik
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - ./infra/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - hire-flow
    healthcheck:
      test: ["CMD", "traefik", "healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 3

  postgres-jobs:
    image: postgres:16-alpine
    container_name: hire-flow-postgres-jobs
    environment:
      POSTGRES_USER: hire_flow
      POSTGRES_PASSWORD: hire_flow_dev
      POSTGRES_DB: jobs_db
    ports:
      - "5432:5432"
    volumes:
      - postgres-jobs-data:/var/lib/postgresql/data
      - ./infra/postgres/init-jobs.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - hire-flow
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hire_flow -d jobs_db"]
      interval: 5s
      timeout: 3s
      retries: 5

  postgres-payments:
    image: postgres:16-alpine
    container_name: hire-flow-postgres-payments
    environment:
      POSTGRES_USER: hire_flow
      POSTGRES_PASSWORD: hire_flow_dev
      POSTGRES_DB: payments_db
    ports:
      - "5433:5432"
    volumes:
      - postgres-payments-data:/var/lib/postgresql/data
      - ./infra/postgres/init-payments.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - hire-flow
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hire_flow -d payments_db"]
      interval: 5s
      timeout: 3s
      retries: 5

  mysql:
    image: mysql:8.4
    container_name: hire-flow-mysql
    environment:
      MYSQL_ROOT_PASSWORD: hire_flow_dev
      MYSQL_DATABASE: contracts_db
      MYSQL_USER: hire_flow
      MYSQL_PASSWORD: hire_flow_dev
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./infra/mysql/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - hire-flow
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "hire_flow", "-phire_flow_dev"]
      interval: 5s
      timeout: 3s
      retries: 5

  nats:
    image: nats:2-alpine
    container_name: hire-flow-nats
    command: ["-c", "/etc/nats/nats-server.conf"]
    ports:
      - "4222:4222"
      - "8222:8222"
    volumes:
      - nats-data:/data/jetstream
      - ./infra/nats/nats-server.conf:/etc/nats/nats-server.conf:ro
    networks:
      - hire-flow
    healthcheck:
      test: ["CMD", "nats-server", "--signal", "ldm=localhost:4222"]
      interval: 5s
      timeout: 3s
      retries: 5

  qdrant:
    image: qdrant/qdrant:v1.13.2
    container_name: hire-flow-qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    networks:
      - hire-flow
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:6333/healthz"]
      interval: 5s
      timeout: 3s
      retries: 5
```

**Step 2: Verify infrastructure boots**

```bash
docker compose up -d traefik postgres-jobs postgres-payments mysql nats qdrant
docker compose ps   # all should show "healthy" after ~30s
```

Expected: all 6 containers running and healthy.

**Step 3: Verify Traefik dashboard**

```bash
curl -s http://localhost:8080/api/overview | head -c 200
```

Expected: JSON response from Traefik API.

**Step 4: Tear down and commit**

```bash
docker compose down
git add compose.yaml
git commit -m "feat(m0): compose.yaml with infrastructure services"
```

---

## Task 4: jobs-api Skeleton (Echo)

**Files:**
- Create: `services/jobs-api/go.mod`
- Create: `services/jobs-api/cmd/server/main.go`
- Create: `services/jobs-api/Dockerfile`

**Step 1: Initialize Go module**

```bash
cd services/jobs-api
go mod init github.com/copycatsh/hire-flow/services/jobs-api
```

Edit `go.mod` to set Go version:

```
// file: services/jobs-api/go.mod
module github.com/copycatsh/hire-flow/services/jobs-api

go 1.24
```

**Step 2: Write main.go**

```go
// file: services/jobs-api/cmd/server/main.go
package main

import (
	"log/slog"
	"net/http"
	"os"

	"github.com/labstack/echo/v4"
)

func main() {
	e := echo.New()
	e.HideBanner = true

	e.GET("/health", func(c echo.Context) error {
		return c.JSON(http.StatusOK, map[string]string{"status": "ok"})
	})

	port := ":8001"
	slog.Info("starting jobs-api", "port", port)
	if err := e.Start(port); err != nil && err != http.ErrServerClosed {
		slog.Error("server failed", "error", err)
		os.Exit(1)
	}
}
```

**Step 3: Download dependencies**

```bash
cd services/jobs-api
go mod tidy
```

**Step 4: Verify it compiles**

```bash
cd services/jobs-api
go build ./cmd/server/
```

Expected: no errors, binary created.

**Step 5: Write Dockerfile**

```dockerfile
# file: services/jobs-api/Dockerfile
FROM golang:1.24-alpine AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /server ./cmd/server

FROM alpine:3.21
RUN apk add --no-cache wget
COPY --from=build /server /server
EXPOSE 8001
CMD ["/server"]
```

**Step 6: Verify Docker build**

```bash
docker build -t hire-flow-jobs-api services/jobs-api/
```

Expected: successful build.

**Step 7: Quick smoke test**

```bash
docker run -d --name test-jobs-api -p 8001:8001 hire-flow-jobs-api
curl -s http://localhost:8001/health
docker rm -f test-jobs-api
```

Expected: `{"status":"ok"}`

**Step 8: Commit**

```bash
git add services/jobs-api/
git commit -m "feat(m0): jobs-api skeleton — Echo /health endpoint"
```

---

## Task 5: contracts Skeleton (Chi)

**Files:**
- Create: `services/contracts/go.mod`
- Create: `services/contracts/cmd/server/main.go`
- Create: `services/contracts/Dockerfile`

**Step 1: Initialize Go module**

```bash
cd services/contracts
go mod init github.com/copycatsh/hire-flow/services/contracts
```

Edit `go.mod`:

```
module github.com/copycatsh/hire-flow/services/contracts

go 1.24
```

**Step 2: Write main.go**

```go
// file: services/contracts/cmd/server/main.go
package main

import (
	"encoding/json"
	"log/slog"
	"net/http"
	"os"

	"github.com/go-chi/chi/v5"
)

func main() {
	r := chi.NewRouter()

	r.Get("/health", func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(map[string]string{"status": "ok"})
	})

	port := ":8003"
	slog.Info("starting contracts", "port", port)
	if err := http.ListenAndServe(port, r); err != nil {
		slog.Error("server failed", "error", err)
		os.Exit(1)
	}
}
```

**Step 3: Download dependencies + verify build**

```bash
cd services/contracts
go mod tidy
go build ./cmd/server/
```

**Step 4: Write Dockerfile**

```dockerfile
# file: services/contracts/Dockerfile
FROM golang:1.24-alpine AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /server ./cmd/server

FROM alpine:3.21
RUN apk add --no-cache wget
COPY --from=build /server /server
EXPOSE 8003
CMD ["/server"]
```

**Step 5: Verify Docker build + smoke test**

```bash
docker build -t hire-flow-contracts services/contracts/
docker run -d --name test-contracts -p 8003:8003 hire-flow-contracts
curl -s http://localhost:8003/health
docker rm -f test-contracts
```

Expected: `{"status":"ok"}`

**Step 6: Commit**

```bash
git add services/contracts/
git commit -m "feat(m0): contracts skeleton — Chi /health endpoint"
```

---

## Task 6: payments Skeleton (Gin)

**Files:**
- Create: `services/payments/go.mod`
- Create: `services/payments/cmd/server/main.go`
- Create: `services/payments/Dockerfile`

**Step 1: Initialize Go module**

```bash
cd services/payments
go mod init github.com/copycatsh/hire-flow/services/payments
```

Edit `go.mod`:

```
module github.com/copycatsh/hire-flow/services/payments

go 1.24
```

**Step 2: Write main.go**

```go
// file: services/payments/cmd/server/main.go
package main

import (
	"log/slog"
	"net/http"
	"os"

	"github.com/gin-gonic/gin"
)

func main() {
	gin.SetMode(gin.ReleaseMode)
	r := gin.New()

	r.GET("/health", func(c *gin.Context) {
		c.JSON(http.StatusOK, gin.H{"status": "ok"})
	})

	port := ":8004"
	slog.Info("starting payments", "port", port)
	if err := r.Run(port); err != nil {
		slog.Error("server failed", "error", err)
		os.Exit(1)
	}
}
```

**Step 3: Download dependencies + verify build**

```bash
cd services/payments
go mod tidy
go build ./cmd/server/
```

**Step 4: Write Dockerfile**

```dockerfile
# file: services/payments/Dockerfile
FROM golang:1.24-alpine AS build
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /server ./cmd/server

FROM alpine:3.21
RUN apk add --no-cache wget
COPY --from=build /server /server
EXPOSE 8004
CMD ["/server"]
```

**Step 5: Verify Docker build + smoke test**

```bash
docker build -t hire-flow-payments services/payments/
docker run -d --name test-payments -p 8004:8004 hire-flow-payments
curl -s http://localhost:8004/health
docker rm -f test-payments
```

Expected: `{"status":"ok"}`

**Step 6: Commit**

```bash
git add services/payments/
git commit -m "feat(m0): payments skeleton — Gin /health endpoint"
```

---

## Task 7: BFF Services x3 (stdlib)

All three BFF services are identical except for service name and port.

**Files:**
- Create: `services/bff/client/go.mod`
- Create: `services/bff/client/cmd/server/main.go`
- Create: `services/bff/client/Dockerfile`
- Create: `services/bff/freelancer/go.mod`
- Create: `services/bff/freelancer/cmd/server/main.go`
- Create: `services/bff/freelancer/Dockerfile`
- Create: `services/bff/admin/go.mod`
- Create: `services/bff/admin/cmd/server/main.go`
- Create: `services/bff/admin/Dockerfile`

**Step 1: Create bff-client**

```go
// file: services/bff/client/go.mod
module github.com/copycatsh/hire-flow/services/bff/client

go 1.24
```

```go
// file: services/bff/client/cmd/server/main.go
package main

import (
	"encoding/json"
	"log/slog"
	"net/http"
	"os"
)

func main() {
	mux := http.NewServeMux()

	mux.HandleFunc("GET /health", func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(map[string]string{"status": "ok"})
	})

	port := ":8010"
	slog.Info("starting bff-client", "port", port)
	if err := http.ListenAndServe(port, mux); err != nil {
		slog.Error("server failed", "error", err)
		os.Exit(1)
	}
}
```

```dockerfile
# file: services/bff/client/Dockerfile
FROM golang:1.24-alpine AS build
WORKDIR /app
COPY go.mod ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /server ./cmd/server

FROM alpine:3.21
RUN apk add --no-cache wget
COPY --from=build /server /server
EXPOSE 8010
CMD ["/server"]
```

**Step 2: Create bff-freelancer (same pattern, port 8011)**

```go
// file: services/bff/freelancer/go.mod
module github.com/copycatsh/hire-flow/services/bff/freelancer

go 1.24
```

```go
// file: services/bff/freelancer/cmd/server/main.go
package main

import (
	"encoding/json"
	"log/slog"
	"net/http"
	"os"
)

func main() {
	mux := http.NewServeMux()

	mux.HandleFunc("GET /health", func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(map[string]string{"status": "ok"})
	})

	port := ":8011"
	slog.Info("starting bff-freelancer", "port", port)
	if err := http.ListenAndServe(port, mux); err != nil {
		slog.Error("server failed", "error", err)
		os.Exit(1)
	}
}
```

```dockerfile
# file: services/bff/freelancer/Dockerfile
FROM golang:1.24-alpine AS build
WORKDIR /app
COPY go.mod ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /server ./cmd/server

FROM alpine:3.21
RUN apk add --no-cache wget
COPY --from=build /server /server
EXPOSE 8011
CMD ["/server"]
```

**Step 3: Create bff-admin (same pattern, port 8012)**

```go
// file: services/bff/admin/go.mod
module github.com/copycatsh/hire-flow/services/bff/admin

go 1.24
```

```go
// file: services/bff/admin/cmd/server/main.go
package main

import (
	"encoding/json"
	"log/slog"
	"net/http"
	"os"
)

func main() {
	mux := http.NewServeMux()

	mux.HandleFunc("GET /health", func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		json.NewEncoder(w).Encode(map[string]string{"status": "ok"})
	})

	port := ":8012"
	slog.Info("starting bff-admin", "port", port)
	if err := http.ListenAndServe(port, mux); err != nil {
		slog.Error("server failed", "error", err)
		os.Exit(1)
	}
}
```

```dockerfile
# file: services/bff/admin/Dockerfile
FROM golang:1.24-alpine AS build
WORKDIR /app
COPY go.mod ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /server ./cmd/server

FROM alpine:3.21
RUN apk add --no-cache wget
COPY --from=build /server /server
EXPOSE 8012
CMD ["/server"]
```

**Step 4: Verify all three compile**

```bash
cd services/bff/client && go build ./cmd/server/ && cd -
cd services/bff/freelancer && go build ./cmd/server/ && cd -
cd services/bff/admin && go build ./cmd/server/ && cd -
```

**Step 5: Verify go.work syncs**

From repo root:

```bash
go work sync
```

Expected: no errors. All 6 Go modules linked.

**Step 6: Commit**

```bash
git add services/bff/
git commit -m "feat(m0): BFF skeletons x3 — stdlib /health endpoints"
```

---

## Task 8: ai-matching Skeleton (FastAPI)

**Files:**
- Create: `services/ai-matching/pyproject.toml`
- Create: `services/ai-matching/src/__init__.py`
- Create: `services/ai-matching/src/main.py`
- Create: `services/ai-matching/Dockerfile`

**Step 1: Create pyproject.toml**

```toml
# file: services/ai-matching/pyproject.toml
[project]
name = "ai-matching"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
```

**Step 2: Create main.py**

```python
# file: services/ai-matching/src/__init__.py
```

```python
# file: services/ai-matching/src/main.py
from fastapi import FastAPI

app = FastAPI(title="ai-matching")


@app.get("/health")
async def health():
    return {"status": "ok"}
```

**Step 3: Write Dockerfile**

```dockerfile
# file: services/ai-matching/Dockerfile
FROM python:3.13-slim

WORKDIR /app

COPY pyproject.toml .
RUN pip install --no-cache-dir .

COPY src/ src/

EXPOSE 8002

CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8002"]
```

**Step 4: Verify Docker build + smoke test**

```bash
docker build -t hire-flow-ai-matching services/ai-matching/
docker run -d --name test-ai-matching -p 8002:8002 hire-flow-ai-matching
curl -s http://localhost:8002/health
docker rm -f test-ai-matching
```

Expected: `{"status":"ok"}`

**Step 5: Commit**

```bash
git add services/ai-matching/
git commit -m "feat(m0): ai-matching skeleton — FastAPI /health endpoint"
```

---

## Task 9: Add All Services to compose.yaml

**Files:**
- Modify: `compose.yaml` — append service definitions after infrastructure

**Step 1: Add all 7 services to compose.yaml**

Append to the `services:` section in compose.yaml (after the infrastructure services):

```yaml
  # ── Core Services ───────────────────────────────────────────

  jobs-api:
    build:
      context: ./services/jobs-api
    container_name: hire-flow-jobs-api
    ports:
      - "8001:8001"
    networks:
      - hire-flow
    depends_on:
      postgres-jobs:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8001/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  ai-matching:
    build:
      context: ./services/ai-matching
    container_name: hire-flow-ai-matching
    ports:
      - "8002:8002"
    networks:
      - hire-flow
    depends_on:
      qdrant:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8002/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  contracts:
    build:
      context: ./services/contracts
    container_name: hire-flow-contracts
    ports:
      - "8003:8003"
    networks:
      - hire-flow
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8003/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  payments:
    build:
      context: ./services/payments
    container_name: hire-flow-payments
    ports:
      - "8004:8004"
    networks:
      - hire-flow
    depends_on:
      postgres-payments:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8004/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  # ── BFF Layer ───────────────────────────────────────────────

  bff-client:
    build:
      context: ./services/bff/client
    container_name: hire-flow-bff-client
    ports:
      - "8010:8010"
    networks:
      - hire-flow
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bff-client.rule=PathPrefix(`/client`)"
      - "traefik.http.routers.bff-client.entrypoints=web"
      - "traefik.http.middlewares.strip-client.stripprefix.prefixes=/client"
      - "traefik.http.routers.bff-client.middlewares=strip-client"
      - "traefik.http.services.bff-client.loadbalancer.server.port=8010"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8010/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  bff-freelancer:
    build:
      context: ./services/bff/freelancer
    container_name: hire-flow-bff-freelancer
    ports:
      - "8011:8011"
    networks:
      - hire-flow
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bff-freelancer.rule=PathPrefix(`/freelancer`)"
      - "traefik.http.routers.bff-freelancer.entrypoints=web"
      - "traefik.http.middlewares.strip-freelancer.stripprefix.prefixes=/freelancer"
      - "traefik.http.routers.bff-freelancer.middlewares=strip-freelancer"
      - "traefik.http.services.bff-freelancer.loadbalancer.server.port=8011"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8011/health"]
      interval: 5s
      timeout: 3s
      retries: 5

  bff-admin:
    build:
      context: ./services/bff/admin
    container_name: hire-flow-bff-admin
    ports:
      - "8012:8012"
    networks:
      - hire-flow
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.bff-admin.rule=PathPrefix(`/admin`)"
      - "traefik.http.routers.bff-admin.entrypoints=web"
      - "traefik.http.middlewares.strip-admin.stripprefix.prefixes=/admin"
      - "traefik.http.routers.bff-admin.middlewares=strip-admin"
      - "traefik.http.services.bff-admin.loadbalancer.server.port=8012"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8012/health"]
      interval: 5s
      timeout: 3s
      retries: 5
```

**Step 2: Validate compose syntax**

```bash
docker compose config --quiet
```

Expected: no errors.

**Step 3: Commit**

```bash
git add compose.yaml
git commit -m "feat(m0): add all services to compose.yaml with healthchecks + Traefik labels"
```

---

## Task 10: Makefile

**Files:**
- Create: `Makefile`

**Step 1: Create Makefile**

```makefile
# file: Makefile
.PHONY: up down health logs test migrate build

# Start all services
up:
	docker compose up -d --build

# Stop all services
down:
	docker compose down

# Check all services respond to /health
health:
	@echo "Checking service health..."
	@failed=0; \
	for svc in \
		"jobs-api:8001" \
		"ai-matching:8002" \
		"contracts:8003" \
		"payments:8004" \
		"bff-client:8010" \
		"bff-freelancer:8011" \
		"bff-admin:8012"; \
	do \
		name=$${svc%%:*}; \
		port=$${svc##*:}; \
		if curl -sf "http://localhost:$$port/health" > /dev/null 2>&1; then \
			printf "  %-20s ✓ healthy\n" "$$name"; \
		else \
			printf "  %-20s ✗ unhealthy\n" "$$name"; \
			failed=1; \
		fi; \
	done; \
	echo ""; \
	if [ "$$failed" = "1" ]; then \
		echo "FAIL: some services are unhealthy"; \
		exit 1; \
	else \
		echo "OK: all services healthy"; \
	fi

# Traefik routing check (BFF via gateway)
health-traefik:
	@echo "Checking Traefik routing..."
	@for path in client freelancer admin; do \
		if curl -sf "http://localhost:80/$$path/health" > /dev/null 2>&1; then \
			printf "  %-20s ✓ routed\n" "/$$path/health"; \
		else \
			printf "  %-20s ✗ not routed\n" "/$$path/health"; \
		fi; \
	done

# Show logs for a specific service (usage: make logs s=jobs-api)
logs:
	docker compose logs -f $(s)

# Run all Go tests (via go workspace)
test:
	go test ./...

# Run migrations (placeholder for M1+)
migrate:
	@echo "No migrations to run (M0 skeleton)"

# Build all images without starting
build:
	docker compose build
```

**Step 2: Verify Makefile syntax**

```bash
make -n up
make -n health
```

Expected: prints the commands without executing them.

**Step 3: Commit**

```bash
git add Makefile
git commit -m "feat(m0): Makefile — up, down, health, logs, test, migrate targets"
```

---

## Task 11: Integration Verification

**Step 1: Build and start everything**

```bash
make up
```

Expected: all containers build and start. Wait ~30s for healthchecks to settle.

**Step 2: Check Docker health status**

```bash
docker compose ps
```

Expected: all 13 containers showing "healthy" status.

**Step 3: Run make health**

```bash
make health
```

Expected output:

```
Checking service health...
  jobs-api             ✓ healthy
  ai-matching          ✓ healthy
  contracts            ✓ healthy
  payments             ✓ healthy
  bff-client           ✓ healthy
  bff-freelancer       ✓ healthy
  bff-admin            ✓ healthy

OK: all services healthy
```

**Step 4: Check Traefik routing**

```bash
make health-traefik
```

Expected:

```
Checking Traefik routing...
  /client/health       ✓ routed
  /freelancer/health   ✓ routed
  /admin/health        ✓ routed
```

**Step 5: Check Traefik dashboard**

```bash
curl -sf http://localhost:8080/api/http/routers | python3 -m json.tool | head -20
```

Expected: JSON showing bff-client, bff-freelancer, bff-admin routers.

**Step 6: Verify merge criteria**

```bash
make up && make health
```

Expected: exits with code 0 — all green.

**Step 7: Tear down**

```bash
make down
```

**Step 8: Final commit if any fixes were needed**

```bash
git add -A
git commit -m "fix(m0): integration fixes from final verification"
```

Only commit if there were changes. If `make up && make health` passed on first try, skip this step.

---

## Task Summary

```
Task  Description                          Files  Est.
────────────────────────────────────────────────────────
  1   Foundation (dirs, go.work, CLAUDE.md)   4    5 min
  2   Infrastructure configs                  5    3 min
  3   compose.yaml (infra only)               1    5 min
  4   jobs-api skeleton (Echo)                3    5 min
  5   contracts skeleton (Chi)                3    5 min
  6   payments skeleton (Gin)                 3    5 min
  7   BFF x3 skeletons (stdlib)               9    8 min
  8   ai-matching skeleton (FastAPI)          4    5 min
  9   Add services to compose.yaml            1    5 min
 10   Makefile                                1    3 min
 11   Integration verification                0    5 min
────────────────────────────────────────────────────────
                                  Total:     34   ~54 min
```

## Merge Criteria

```bash
make up && make health
# Exit code 0, all 7 services report ✓ healthy
```
